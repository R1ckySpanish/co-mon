all:
  vars:
    ansible_user: ansible
    ansible_ssh_private_key_file: /home/ansible/.ssh/ansible.key
    env:
      env:
    
    
  children:
    vps:
      hosts:
        ru:
          ansible_host: ${{ vars.HOSTS['ru'] }}
        kz:
          ansible_host: ${{ vars.HOSTS['kz'] }}
        eu:
          ansible_host: ${{ vars.HOSTS['eu'] }}
          
    main_node:
      hosts:
        ru:
          ansible_host: ${{ vars.HOSTS['ru'] }}

       
    vm_agent:
      hosts:
        kz:
          ansible_host: ${{ vars.HOSTS['kz'] }}

        
